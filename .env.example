# WaveSpeed API Key（在 https://wavespeed.ai/ 注册获取）
WAVESPEED_API_KEY=your_wavespeed_api_key_here

# JWT 密钥（建议修改为随机字符串，可用命令生成：node -e "const c=require('crypto');console.log(c.randomBytes(32).toString('hex'))"）
JWT_SECRET=change-this-to-a-random-string

# 服务端口
PORT=3000
